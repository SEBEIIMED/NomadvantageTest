NomadvantageTest :

To run the application, please generate the binary, after you have to type the number of rows and columns of map.
The program runs recursively and displays the state of the map after the current continent fading.
At the end we display the number of continents.

